import React from 'react';

/**
 * Placeholder for the profile module. Kept as its own component so the
 * eventual profile-editing logic has a home without touching Dashboard.jsx.
 */
const ProfileSection = () => {
  return <div className="placeholder-text">Profile module placeholder...</div>;
};

export default ProfileSection;
